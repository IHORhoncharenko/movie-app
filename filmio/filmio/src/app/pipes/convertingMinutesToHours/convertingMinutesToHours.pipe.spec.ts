/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { ConvertingMinutesToHoursPipe } from './convertingMinutesToHours.pipe';

describe('Pipe: ConvertingMinutesToHourse', () => {
  it('create an instance', () => {
    let pipe = new ConvertingMinutesToHoursPipe();
    expect(pipe).toBeTruthy();
  });
});
